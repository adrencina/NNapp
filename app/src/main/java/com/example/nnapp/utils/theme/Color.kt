package com.example.nnapp.utils.theme

import androidx.compose.ui.graphics.Color

// Paleta de colores basada en el rubro eléctrico (valores extraídos de tu propuesta):
val ElectricBlue = Color(0xDF0B93FF)      // Azul eléctrico (usado como color primario)

val ElectricGreen = Color(0xFF2AD07D)       // Verde (para acentos, estados positivos)
val DarkTextColor = Color(0xFF333333)       // Gris oscuro (texto principal sobre fondo claro)
val White = Color(0xFFFFFFFF)               // Blanco (fondo principal)

val NegroOscuro = Color(0xE62F2F2F)

// Roles de color para el tema en modo claro:
val PrimaryColor = ElectricBlue
val SecondaryColor = ElectricGreen

val BackgroundColor = White // Fondo principal en modo claro
val ElectricGrayLight = Color(0xD9888888)   // Gris claro (bordes y textos cards y botones)


val TextColor = DarkTextColor